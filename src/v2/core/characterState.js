/**
 * V2 Character System - State
 * Runtime state for the V2 character (equipment lives in V2 inventory, not here).
 */

export let characterV2 = null;
export function setCharacterV2(val) { characterV2 = val; }
